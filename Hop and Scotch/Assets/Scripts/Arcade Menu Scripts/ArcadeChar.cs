using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ArcadeChar : MonoBehaviour
{
    [SerializeField] public string AnimationCode;
    [SerializeField] public string ArcadePath;
    [SerializeField] public Sprite CharacterArt;
    [SerializeField] public Sprite CharacterProfile;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
